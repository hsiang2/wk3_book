import React from "react";
import { Text, Box } from "native-base";

const MyBooksScreen = () => {
    return (
        <Box flex={1} alignItems="center" justifyContent="center">
            <Text fontSize={20}>My Books</Text>
        </Box>
    );
};

export default MyBooksScreen;
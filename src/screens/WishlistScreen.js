import React from "react";
import { Text, Box } from "native-base";

const WishlistScreen = () => {
    return (
        <Box flex={1} alignItems="center" justifyContent="center">
            <Text fontSize={20}>Wishlist</Text>
        </Box>
    );
};

export default WishlistScreen;